{
  "private": true,
  "workspaces": [
    "hello-react",
    "hello-react-redux",
    "understanding-reducer"
  ],
  "scripts": {
    "format": "prettier --write '**/*.{ts,tsx,json,html,md}' '!**/{.cache,dist}/**'",
    "compile": "tsc"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "typescript": "^3.7.2"
  }
}
