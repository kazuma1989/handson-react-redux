{
  "compilerOptions": {
    // Transpile for latest browsers.
    "target": "esnext",

    // Required for dynamic import.
    "module": "esnext",

    // Some packages need this option.
    "moduleResolution": "node",

    // Use `import React from "react"` instead of `import * as React from "react"`.
    "allowSyntheticDefaultImports": true,

    // Required for Parcel to use JSX.
    "jsx": "react",

    // Useful type checks.
    "strictNullChecks": true,
    "skipLibCheck": true,
    "noEmit": true
  }
}
